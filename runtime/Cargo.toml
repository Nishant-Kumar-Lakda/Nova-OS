[package]
name = "nova-runtime"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "NOVA Action Runtime and Skill Registry"

[dependencies]
nova-nexus = { path = "../nexus" }
nova-platform = { path = "../platform" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
