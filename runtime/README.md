# AIR Runtime

AIR (AI Runtime) will manage local models as system resources.

Initial responsibilities:

- model manifests;
- lifecycle state;
- resource budgets;
- scheduler policy;
- inference backend abstraction;
- cache/eviction policy.

The first implementation is intentionally deferred until the NIL and skill contracts are stable.
