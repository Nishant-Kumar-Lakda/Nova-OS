[package]
name = "nova-memory"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "NOVA local memory subsystem"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
