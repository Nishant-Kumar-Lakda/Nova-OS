[package]
name = "nova-planner"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "NOVA deterministic action graph planner"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
