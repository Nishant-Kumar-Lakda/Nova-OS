[package]
name = "nova-context"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "NOVA active context engine"

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "2"
